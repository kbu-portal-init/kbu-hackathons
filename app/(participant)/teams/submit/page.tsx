import { RoutePlaceholder } from "@/components/route-placeholder";

export default function TeamSubmitPage() {
    return (
        <RoutePlaceholder
            eyebrow="Participant workspace"
            title="Submit your project"
            description="Project submission details, deadlines, and upload requirements will appear here."
        />
    );
}
