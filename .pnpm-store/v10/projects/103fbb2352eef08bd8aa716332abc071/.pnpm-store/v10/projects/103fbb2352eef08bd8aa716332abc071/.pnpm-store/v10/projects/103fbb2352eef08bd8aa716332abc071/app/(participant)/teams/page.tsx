import { RoutePlaceholder } from "@/components/route-placeholder";
export default function TeamsPage() {
    return (
        <RoutePlaceholder
            eyebrow="Participant access"
            title="Team dashboard"
            description="Your approved team workspace will live here. Login and team approval will be connected in a future phase."
        />
    );
}
