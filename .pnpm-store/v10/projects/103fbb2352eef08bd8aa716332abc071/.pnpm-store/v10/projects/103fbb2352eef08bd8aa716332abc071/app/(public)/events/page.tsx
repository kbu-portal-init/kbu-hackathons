import { RoutePlaceholder } from "@/components/route-placeholder";
export default function EventsPage() {
    return (
        <RoutePlaceholder
            eyebrow="Discover"
            title="Hackathon events"
            description="Find upcoming challenges, explore event details, and get ready to build with the KBU community."
        />
    );
}
