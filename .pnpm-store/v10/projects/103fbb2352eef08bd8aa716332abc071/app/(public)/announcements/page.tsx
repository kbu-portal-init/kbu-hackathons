import { RoutePlaceholder } from "@/components/route-placeholder";
export default function AnnouncementsPage() {
    return (
        <RoutePlaceholder
            eyebrow="Stay informed"
            title="Announcements"
            description="Keep up with registration dates, community news, important updates, and everything happening at KBU Hub."
        />
    );
}
